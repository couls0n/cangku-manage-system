spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${WAREHOUSE_DB_URL:jdbc:mysql://localhost:3306/warehouse_db?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai}
    username: ${WAREHOUSE_DB_USERNAME}
    password: ${WAREHOUSE_DB_PASSWORD}
